version: "3.8"
services:
  postgres:
    image: ghcr.io/pgvector/pgvector:postgres15-0.7.0
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: nourisher
    ports:
      - "5432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data
